import 'package:flutter/material.dart';

const color1 = {"color": Color(0xFF7C008A)};
const color2 = {"color": Color(0xFFe42d29)};
const color3 = {"color": Color(0xFF27b737)};

var subjectBox = BoxDecoration(
    color: Colors.deepPurple[50],
    border: Border.all(color: Color(0xFF7C008A),width: 1),
    borderRadius: BorderRadius.all(Radius.circular(30))
);
var marginBox =  const EdgeInsets.all(10);
const titleSubject = TextStyle(fontSize: 18);
